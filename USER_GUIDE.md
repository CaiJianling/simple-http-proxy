# 网络代理服务器 - 图形界面版使用指南

## 📋 项目概述

这是一个功能完整的网络代理服务器，提供现代化的图形界面，支持实时监控、日志查看、流量统计等功能。提供两个版本：完整版和轻量版。

## 🚀 快速开始

### 方案一：轻量版（推荐）
无需额外依赖，直接运行：
```bash
python proxy_gui_lite.py
```

### 方案二：完整版（需要安装依赖）
```bash
# 安装依赖
pip install -r requirements.txt

# 运行程序
python proxy_gui.py
```

### 方案三：打包成exe文件
```bash
# 轻量版打包
双击运行 build_lite_exe.bat

# 完整版打包  
双击运行 build_exe.bat
```

打包完成后，exe文件会在 `dist` 目录中生成，双击即可运行。

## 🎯 功能特性

### ✅ 核心功能
- **多协议支持**: HTTP/HTTPS/SOCKS5代理
- **参数配置**: 主机地址、端口、客户端数量、缓冲区大小、转发延迟
- **一键控制**: 启动/停止服务器
- **实时状态**: 服务器运行状态指示

### 📊 监控功能
- **连接统计**: 总连接数、活跃连接数
- **流量统计**: 发送/接收字节数、实时流量速率
- **运行时间**: 服务器运行时长显示
- **连接列表**: 实时显示所有活跃连接信息

### 📈 可视化功能
- **实时日志**: 彩色标识不同类型日志
- **流量图表**: 动态显示网络流量趋势（轻量版使用自定义Canvas实现）
- **状态指示**: 直观的服务器状态显示

## 🖥️ 界面布局

```
┌─────────────────────────────────────────────────────────────────────┐
│                    服务器控制面板                                    │
│  主机地址:[0.0.0.0] 端口:[8080] 最大客户端:[100] [▶ 启动服务器]      │
│  缓冲区大小:[8192] 转发延迟(ms):[0]          [⏹ 停止服务器]      │
├─────────────────────┬───────────────────────────────────────────────┤
│   服务器状态        │               实时日志                        │
│  ● 运行状态         │  [14:30:25] 服务器启动成功                   │
│  总连接数: 15       │  [14:30:26] New connection from 127.0.0.1  │
│  活跃连接: 3         │  [14:30:27] Connection closed                │
│  发送流量: 1.2 MB    │  [14:30:28] New connection from 192.168.1.5  │
│  接收流量: 856 KB    │                                              │
│  运行时间: 00:05:30  │                                              │
├─────────────────────┼───────────────────────────────────────────────┤
│     活跃连接         │                                               │
│  IP地址   端口 时间  │                                               │
│ 127.0.0.1 8080 14:30 │                                               │
│ 192.168.1.5 8080 14:31│                                               │
├─────────────────────┴───────────────────────────────────────────────┤
│                    流量监控图表                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    [实时流量折线图]                         │   │
│  │     发送流量: ████████████  接收流量: ████████████       │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

## ⚙️ 参数说明

| 参数 | 说明 | 默认值 | 范围 |
|------|------|--------|------|
| 主机地址 | 服务器监听地址 | 0.0.0.0 | IP地址 |
| 端口 | 服务器监听端口 | 8080 | 1-65535 |
| 最大客户端 | 最大并发连接数 | 100 | 1-1000 |
| 缓冲区大小 | 数据缓冲区大小 | 8192 | 1024-65536 |
| 转发延迟 | 数据转发延迟时间 | 0 | 0-10000ms |

## 🎨 界面特色

### 颜色标识
- **🟢 绿色**: 连接成功、状态正常
- **🔴 红色**: 错误信息、停止状态
- **🔵 蓝色**: 一般信息、提示
- **🟡 黄色**: 警告信息

### 图标说明
- **▶**: 启动按钮
- **⏹**: 停止按钮
- **●**: 状态指示器

## 🔧 使用步骤

### 1. 启动程序
- 双击exe文件，或
- 命令行运行 `python proxy_gui_lite.py`

### 2. 配置参数
- 设置主机地址（通常保持默认0.0.0.0）
- 设置监听端口（默认8080）
- 调整其他参数（可选）

### 3. 启动服务器
- 点击"▶ 启动服务器"按钮
- 观察状态指示器变为绿色
- 查看实时日志确认启动成功

### 4. 监控运行
- 观察流量图表变化
- 查看活跃连接列表
- 监控统计信息更新

### 5. 停止服务器
- 点击"⏹ 停止服务器"按钮
- 等待状态指示器变为红色
- 程序可安全关闭

## 📊 性能指标

### 监控指标
- **连接数**: 实时显示总连接和活跃连接
- **流量统计**: 精确到字节的流量统计
- **响应时间**: 服务器处理请求的响应时间
- **错误率**: 连接失败和错误的统计

### 图表功能
- **实时更新**: 每秒更新一次数据
- **历史记录**: 保存最近60个数据点
- **缩放适应**: 自动调整图表比例
- **双曲线**: 分别显示发送和接收流量

## 🛡️ 安全注意事项

### 网络安全
- 确保防火墙允许监听端口
- 避免在公网暴露敏感端口
- 定期检查活跃连接列表

### 系统安全
- 以普通用户权限运行（避免管理员权限）
- 监控系统资源使用情况
- 及时关闭不需要的服务

## 🔍 故障排除

### 常见问题

#### 1. 端口被占用
```
错误: 服务器启动失败，请检查端口是否被占用
```
**解决方法**: 
- 更换端口号
- 检查是否有其他程序占用该端口
- 使用命令 `netstat -ano | findstr 端口号` 查看占用情况

#### 2. 权限不足
```
错误: 无法绑定到指定端口
```
**解决方法**:
- 使用1024以上端口
- 以管理员身份运行程序
- 检查防火墙设置

#### 3. 依赖缺失
```
错误: 找不到模块 tkinter
```
**解决方法**:
- 安装完整版Python（包含tkinter）
- 使用轻量版程序（proxy_gui_lite.py）

### 日志分析
- **连接日志**: 显示客户端连接和断开信息
- **错误日志**: 红色显示，包含错误详情
- **流量日志**: 显示数据传输统计

## 📈 扩展功能

### 未来计划
- [ ] 配置文件保存和加载
- [ ] 更多代理协议支持
- [ ] 用户认证功能
- [ ] 数据加密传输
- [ ] 远程管理界面
- [ ] 性能优化算法

### 自定义开发
程序采用模块化设计，易于扩展：
- `ProxyServer` 类: 核心服务器功能
- `ProxyGUI` 类: 图形界面功能
- `TrafficCanvas` 类: 流量图表组件

## 📞 技术支持

如遇到问题，请检查：
1. ✅ Python版本是否为3.7+
2. ✅ 端口是否被占用
3. ✅ 防火墙设置是否正确
4. ✅ 依赖包是否安装完整
5. ✅ 程序权限是否足够

## 📄 许可证

MIT License - 详见项目根目录的LICENSE文件

---

**💡 提示**: 建议首次使用时先运行轻量版，确认基本功能正常后再考虑完整版。